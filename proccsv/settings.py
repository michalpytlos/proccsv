import os
from proccsv import package_directory

INPUT_DIR_PATH = os.path.join(package_directory, 'input')
OUTPUT_DIR_PATH = os.path.join(package_directory, 'output')
OUTPUT_FILE_NAME_SUFFIX = '_out'
